/**
 * Reusable UI Components
 *
 * Export all shared UI components from this barrel file.
 * Each component should be self-contained and accept props for customization.
 *
 * Components to implement:
 * - Button (primary, secondary, ghost, danger variants)
 * - Card (with header, body, footer slots)
 * - Badge (status, urgency, category badges)
 * - Modal (dialog with overlay)
 * - Input (text, textarea, select, file)
 * - Spinner (loading indicator)
 * - Avatar (user avatar with fallback)
 * - Skeleton (loading placeholder)
 * - EmptyState (no data placeholder)
 * - Tooltip (hover tooltip)
 * - ProgressBar (progress indicator)
 * - StatCard (metric card with icon)
 */

// Export components as they are created
// export { default as Button } from './Button';
// export { default as Card } from './Card';
// export { default as Badge } from './Badge';
// export { default as Modal } from './Modal';
// export { default as Spinner } from './Spinner';
// export { default as StatCard } from './StatCard';
