name: Deploy to Netlify

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: netlify
      url: http://www.getreplate.qzz.io

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deployment Complete
        run: echo "Deployed"